# Core Dependencies - CUDA 12.1 Environment
gradio>=4.0.0
# PyTorch は Dockerfileで明示的にインストール: torch==2.3.1 torchvision --index-url https://download.pytorch.org/whl/cu121
# torch==2.3.1  # Dockerfileで明示的にインストールされるためコメントアウト
# torchvision   # Dockerfileで明示的にインストールされるためコメントアウト
torchaudio
transformers>=4.20.0
# numpy==1.26.4  # Dockerfileで明示的にバージョン指定されるためコメントアウト

# Configuration and Data Processing
python-box>=7.0.0
omegaconf>=2.2.0
pyyaml>=6.0

# Machine Learning - PyTorch Lightning & AI Libraries
pytorch_lightning>=2.0.0
lightning>=2.0.0
einops>=0.6.0
timm>=0.9.0
# flash_attn   # Dockerfileで ninja, triton と共にインストール
huggingface_hub>=0.15.0
# spconv-cu121  # Dockerfileで明示的にインストール
# torch_scatter torch_cluster  # Dockerfileで明示的にインストール

# 3D Processing
trimesh>=3.20.0
open3d>=0.16.0
fast-simplification>=0.1.0
bpy==4.2.*

# Graphics and Visualization
pyrender>=0.1.45

# Utilities
addict>=2.4.0